## 1. 核查与基线

- [x] 1.1 按 `build_node_groups()` 完成七个几何组、共享函数与材质消费者的属性存取清单，记录每次写入的域、类型、Selection、版本、消费位置及保留或替换依据，在本变更中交付核查结果。
- [x] 1.2 运行现有节点测试，记录各组 Store/Named/Capture 节点数量及代表场景的几何、UV 和材质属性基线，确认测试正常退出并区分已有失败。

## 2. 整理节点字段传递

- [x] 2.1 将 Depth Cutout 受影响的阶段连接改为显式函数返回值与 socket 引用，移除自动编号定位和输入反查；用现有 Depth Cutout 求值测试验证连接等价。
- [x] 2.2 简化共用 Depth Surface 的面中心、面相机值和投影相机值传递，显式保持域转换；用非均匀面深度、Split 开关和零厚度测试验证结果。
- [x] 2.3 为分裂、删面与厚度构建中必须保留的源位置、角值、切分标记、顶点身份和法线建立必要捕获，迁移调用方；通过断层封口、独立重叠表面和法线测试验证快照传播。
- [x] 2.4 将 Depth Cutout 的中心 seed 直接接 Blur，并按属性版本简化局部体积计算、保存必要表面快照；通过 Balloon/Uniform、厚度连续性、边缘圆化、前表面膨胀和法线平滑测试验证行为。
- [x] 2.5 逐项简化 Panorama 的方向、UV、距离和无效标记传递，保持 POINT/FACE 聚合和球面采样时机；通过周期接缝、极点、无效深度边缘、Valid Only 与 Split 测试验证行为。
- [x] 2.6 按清单完成 Image Plane、Relief Plane、Image Cutout、Depth Balloon 及材质边界检查，保留必要 UV 与命名协议；通过对应节点和材质测试确认域、属性值及着色输入一致。
- [x] 2.7 删除失去业务用途的存取函数、读节点和清理节点，调整依赖中间属性的测试；检索确认旧调用消失，并验证所有保留的临时命名属性在最后消费者后清理。

## 3. 更新规范与布局验证

- [x] 3.1 按设计更新 `AGENTS.md` 与 `docs/internals/node-assets.md`，明确直连、域转换、捕获、命名协议与显式接线的选择依据；检查两处规范一致且已替换跨区域即存储的旧规则。
- [x] 3.2 验证现有排列器对直连和匿名属性的处理，仅修复实际出现的布局问题；通过 arrangement 与 saved layout 测试，并检查各组字段连线可读、未使用输出隐藏、接口顺序与默认值一致。

## 4. 重建与整体交付

- [x] 4.1 运行 `uv run node-group build`，更新 `src/anyimage/assets/O_AnyImage.blend`，确认构建、保存后独立 Blender 加载和求值验证全部成功。
- [x] 4.2 运行 `uv run pytest` 并确认正常退出，对照基线检查相关几何、UV、材质属性和修改器接口结果，修复本次变更引入的失败。
- [x] 4.3 检查最终差异、旧引用和临时属性清理，交付逐组调整结论、存取节点数量变化及测试结果，确认源码、测试、规范和 `.blend` 资产同步完成。
