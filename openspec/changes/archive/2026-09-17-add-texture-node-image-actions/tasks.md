## 1. 图片编辑目标

- [x] 1.1 在 common 集中解析 Empty 与材质纹理节点目标，捕获源 Image、编辑树身份和播放参数；测试材质固定、节点组编辑、无效上下文和节点改名。
- [x] 1.2 将图片结果加载改为显式 Image 输入并更新真实调用点；通过现有 Empty 图片处理测试，检查旧调用签名无残留。

## 2. 原位提交与任务接入

- [x] 2.1 实现节点图片事务提交，复用内容快照、恢复与打包；测试独占 Image 身份、Fake User、共享节点/Empty 隔离、连线设置保留及失败回滚。
- [x] 2.2 接入现有 Remove Background 和 Upscale 的目标解析、输入准备和响应；测试模型配置、2× 尺寸、输入限制、动画帧参数及任务清理。
- [x] 2.3 在异步响应中校验原树、节点及源绑定；测试切换选择后仍命中原目标，删除、同名重建和改绑图片时安全退出。

## 3. 右键菜单

- [x] 3.1 添加纹理节点 AnyImage 菜单，复用两项操作及 AI 设置入口，显示目标图片的 Upscale 尺寸；测试可见条件、服务忙碌及环境准备入口。
- [x] 3.2 接入节点菜单回调与菜单类型注册、逆序注销；通过注册测试验证重复启用与禁用没有遗留回调。

## 4. 综合验证

- [x] 4.1 使用真实 Blender 测试验证两项异步操作的撤销重做、图片打包保存重开、共享节点组实例与动画播放，确认透明结果及原节点结构正确。
- [x] 4.2 运行 `uv run pytest` 全量测试并确认正常退出，检查最终差异与旧引用；保留既有未提交修改，不构建文档或产物。
