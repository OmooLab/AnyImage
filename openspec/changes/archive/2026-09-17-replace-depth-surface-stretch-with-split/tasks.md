## 1. 固定失败样例与控制语义

- [x] 1.1 将正厚度侧壁脱开缩减为定向正确的流形网格和深度阶跃样例，以边界配对、非流形边和未焊接接缝检查复现失败，记录根因。
- [x] 1.2 增加 Split 简单倒数换算测试，覆盖默认关闭、0.5 对应阈值 5、最大值 1 对应阈值 2.5，以及切边选择随控制值单调增强。
- [x] 1.3 增加原始表面、平面、斜坡、阶跃、等比缩放和零跨度用例，验证面中心 Z 差与实际中心跨度的判据及关闭时的原始投影行为。

## 2. 接入分离与厚度连接

- [x] 2.1 移除内外节点组的 Stretch Limit 接口与专用删几何分支，在 Options 原位置加入 Split；通过接口测试确认范围、默认值、FACTOR、SINGLE 及只连接 Depth Surface。
- [x] 2.2 实现面采样、邻面判别、面角捕获和拆边，复用 Uniform Scale 与 Depth Scale；通过零厚度阶跃测试确认保留全部原始面且两侧独立。
- [x] 2.3 在拆边后固定顶点投影及边界身份，修复厚度侧壁数据传播与连接；通过单段、多段、粗细网格测试确认正背面和侧壁闭合、厚度正确。
- [x] 2.4 限制连接在对应区域内完成；通过间隔小于原合并容差的双区域用例确认不会重新焊接切口。
- [x] 2.5 将 Depth Surface 的 Cleanup 移至 Split Edges 后、投影和厚度前，移除该分支提前和重复清理；通过碎片样例验证按拆后平面面积清理、关闭 Split 仍清理且 Thickness 不改变保留区域。
- [x] 2.6 接入外层 Smooth 和 Shape 分支，通过完整修改器求值测试确认加厚区域保持连接，其他 Shape 的 Cleanup 位置与行为不变。

## 3. 整理节点布局与共享资产

- [x] 3.1 按 AGENTS.md 重排主链和辅助分支，局部放置 Group Input，保留默认标题并隐藏未使用 sockets；在节点编辑器检查可读性，消除可避免的长线、交错和回折。
- [x] 3.2 整理厚度 Repeat 配对区域及必要 Frame，通过实际节点尺寸检查确保无重叠、无遮挡，并保存节点编辑器预览供审阅。
- [x] 3.3 更新节点验证脚本，覆盖接口顺序、SINGLE、subtype、未使用输出隐藏、布局与几何求值；运行验证确认通过。
- [x] 3.4 构建并验证节点资产，检查六个物体共同引用资产节点组、标定与控制各自传入，运行时不新建节点组。

## 4. 综合验收

- [x] 4.1 用蜘蛛与蛛丝、甲虫右侧腿与躯干、噬菌体左下交叉腿检查 High/Low，在 Split 0、0.5、1 及正厚度下保存对比；重点确认中点效果、碎片程度和侧壁无缝连接。
- [x] 4.2 运行 Cutout 定向测试、节点资产验证和相关回归测试，重新打开保存的 Blender 文件复核厚度与共享节点；不能仅以面数或厚度范围通过代替闭合验证。
- [x] 4.3 同步必要的节点资产说明以符合 AGENTS.md，验证构建脚本、资产接口和说明一致，不构建扩展发行包。
- [x] 4.4 运行 OpenSpec 严格校验，确认所有规格场景都有测试或明确的视觉验收记录后再标记实施完成。

## 5. 轮廓与连线修正

- [x] 5.1 只在选中切边的关联顶点应用所属侧深度，沿原相机射线重建位置；验证无切边时投影完全不变、切口外顶点不变、切口顶点保持原射线。
- [x] 5.2 明确排列侧壁、背面、连接及位置恢复分支，复制局部 Position 字段并用 Reroute 通道组织长线；保存实际节点编辑器预览并验证长连线不斜穿计算区域。

## 6. 按处理阶段组织布局

- [x] 6.1 阅读用户的 OmooNodes 参考资产，按几何主轴收拢阶段，属性计算在局部竖排和并行展开，局部重复 Group Input。
- [x] 6.2 验证重排前后运算、默认值和实际连接一致，运行 Cutout 求值检查，并在 Blender 中按实际节点尺寸复核布局、保存预览。

## 7. 命名属性与阶段留白

- [x] 7.1 将跨区域暂存数据改为 o_depth_* 命名属性并就近读取，输出前统一移除，验证字段域、几何结果和属性清理。
- [x] 7.2 拉开大步骤间距，以 String 节点标注阶段，按主轴与局部计算树重排并在 Blender 检查实际布局。
- [x] 7.3 重建资产，复核六物体共享节点与厚度闭合，运行回归和 OpenSpec 校验。

## 8. 双侧计算分支与 Wildcard 清理

- [x] 8.1 用单个 Wildcard Remove Named Attribute 清理 o_depth_*，验证新增匹配属性同样被清理、用户属性保留。
- [x] 8.2 移除 String 注解，将分支在主轴上下错落横向展开，允许必要的几何引用回接，检查实际节点布局。
- [x] 8.3 重建共享资产，比较真实样例几何，运行节点验证、回归与 OpenSpec 校验。

## 9. 全部几何资产布局与规则

- [x] 9.1 回接直接连接，收紧支线与主轴的间距，将同样的布局方式用于资产中的全部七个几何节点组。
- [x] 9.2 将主轴、双侧支线、局部输入、命名属性、Wildcard 清理与视觉检查规则整理进 AGENTS.md，更新节点资产说明。
- [x] 9.3 重建资产，对比运算和连接、执行几何回归，并在 Blender 中逐组检查实际布局，保存预览。

## 10. 紧凑布局与同级输入对齐

- [x] 10.1 收紧全部几何节点组的间距，将同一目的的节点聚拢，并将汇合节点的独立同级来源排齐，菜单分支按输入顺序排列。
- [x] 10.2 同步 AGENTS.md 与布局规范，验证汇合分支对齐、实际尺寸和计算结果，保存预览。

## 11. 按用途聚拢支线与 XYZ 对齐

- [x] 11.1 将 Combine XYZ 纳入同级输入对齐，Depth Plane 同用途计算收在一侧，收紧 Split 选边支线内部间距，同步 AGENTS.md 与布局规范。
- [x] 11.2 重建资产并检查七组实际节点布局，确认计算与连接不变，运行节点验证及相关回归，保存局部预览。

