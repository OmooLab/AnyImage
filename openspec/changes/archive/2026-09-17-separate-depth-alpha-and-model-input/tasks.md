## 1. 模型输入

- [x] 1.1 将共享模型输入准备改为读取 RGB 后缩放，更新输入测试，验证相同 RGB、不同 alpha 在缩放和原尺寸情况下得到相同模型输入。

## 2. alpha 轮廓延伸

- [x] 2.1 将 alpha 轮廓与 mask 来源筛选分开，固定仅由 alpha 决定的修补范围；通过内部低 mask 孔洞及全不透明输入测试确认深度保持原值。
- [x] 2.2 验证轮廓内侧保护带、外侧 4 像素延伸、局部平滑、分量隔离、无可靠来源回退和 XYZ 射线一致性，并确认 alpha 与 validity 未改写。

## 3. 深度产物与消费者

- [x] 3.1 为深度 EXR 写入添加必需的原图 alpha 参数并迁移全部调用；验证尺寸检查、零值、连续乘积和 XYZ 原值保存，确认 GeometryFrame.validity 仍为模型 mask。
- [x] 3.2 更新 Artifact 集成测试，验证 alpha 对齐预测尺寸、Metadata 使用原始模型有效性与可见参考区域、Normal 使用原始预测。
- [x] 3.3 按用户要求保留全部几何节点相关内容；验证 EXR 图像读取保留组合 Alpha 与 XYZ，检查现有校准读取，运行消费者回归测试，节点调整由另一 agent 负责。

## 4. 整体验证

- [x] 4.1 运行针对性测试后执行 `uv run pytest`，确认正常退出；检查最终差异与旧调用引用，保留工作区其他修改，仅报告本变更结果，不同步内部文档或构建资产及扩展包。


验证记录：针对性测试 48 passed、2 subtests passed；全量测试 805 passed、2 xfailed，模型同步模块并行迁移导致 6 项初始化错误。迁移后单独重跑 tests/test_sync_models.py，6 passed，退出码 0。最终差异及写入调用检查通过；本变更未修改几何节点脚本、资产或节点测试。
