## 1. 命令入口

- [x] 1.1 将脚本入口替换为 `node-group`、`model`，增加子命令解析并删除旧入口及 `--prepare-only`；更新入口测试，验证帮助、无动作、非法参数均不执行业务操作。

## 2. 节点组命令

- [x] 2.1 实现 build 与 check 的公共 Blender 调度，支持 `--blender`、`--skip-tests`；通过命令测试验证顺序、check 不构建和失败即停止。
- [x] 2.2 接入 preview，支持默认临时 HTML、`--output`、`--no-open`、`--fragment`，由父进程打开浏览器；验证参数传递、输出内容、片段模式和资产内容保持原样。

## 3. 模型命令

- [x] 3.1 实现 prepare 与 sync 两个模型子命令，prepare 统一下载和导出，sync 复用准备后上传逻辑；通过隔离测试验证缓存命中、下载或转换选择、校验失败阻断上传，以及 prepare 不依赖上传工具。
- [x] 3.2 验证 prepare 覆盖现成模型下载、普通模型转换与 PiSA-SR 导出，测试源权重准备、产物校验、失败临时文件清理和正式目标保护。

## 4. 集成验证

- [x] 4.1 执行 `uv sync` 后检查两组命令及全部子命令的 `--help`，运行相关工具测试，确认安装入口与源码一致。
- [x] 4.2 执行 `uv run node-group build` 验证真实 Blender 构建、资产校验和全量 pytest 正常退出；运行 `node-group check` 和 `node-group preview --no-open`，校验资产哈希保持原样及预览文件可查看。
- [x] 4.3 检查代码与测试中的旧入口引用、最终差异和测试结果，确认保留工作区既有修改，并简要报告命令迁移及验证结果。
