## 1. 参数与偏好

- [x] 1.1 将 Mesh Detail 替换为 Edge Length 长度属性，接通工具栏、菜单与 Operator，验证默认 0.1 m 和场景单位语义。
- [x] 1.2 将最大网格分辨率偏好替换为默认 1% 的 Minimum Relative Edge Length，统一 preferences.py 读取入口，更新属性注册与相关测试。

## 2. 世界空间采样

- [x] 2.1 从有效裁切内容求世界平面轴向包围盒长边，实现包含 Unit Scale 的有效间距计算，覆盖比例限制、旋转、非均匀缩放与透明留白。
- [x] 2.2 将普通轮廓的重采样、内部采样和间距衍生容差接入有效世界间距，移除源像素密度换算链。
- [x] 2.3 将 Fine Outline 的密度判断接入统一世界度量，保持 Alpha 取样、UV 和细小轮廓支持，验证非均匀缩放下的目标间距。
- [x] 2.4 同步与 AI 异步执行路径使用最终内容范围计算有效间距；保持 Blender 数据读取在主线程，分别保留请求值和有效值。

## 3. 反馈与验证

- [x] 3.1 相对限制生效时使用场景长度格式反馈有效值，补充设置值保持不变的交互测试。
- [x] 3.2 更新相关几何测试，覆盖分辨率和画布变化、缩放、两种轮廓模式及细小部件；补充实际 Blender 非默认 Unit Scale 的长度输入与初始化验证。
- [x] 3.3 检查并移除旧档位、像素间距参数和旧偏好引用，运行相关测试并确认正常退出，检查最终差异。
