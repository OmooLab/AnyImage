## 1. RGB 合成修正

- [x] 1.1 复核有效投影、最底层选择和公共采样入口，保留已有几何行为。
- [x] 1.2 以底层原始 RGB 初始化，前景逐层按 Alpha 叠加；Alpha 独立沿用原公式，源外扩色使用最终颜色。

## 2. 验证

- [x] 2.1 更新半透明预期，补充三层合成、白色底图上的前景渐变、预乘插值和单源原始 RGB 测试。
- [x] 2.2 验证实际 Packed Image 在 Mask Add 后保留前景到白色的连续渐变。
- [x] 2.3 运行定向与全量测试，校验 OpenSpec；不改产品文档，不构建产物。

## 验证结果

- 定向测试：51 passed；全量 uv run pytest：440 passed。
- 实际 Packed PNG 与 Mask Add 结果均验证了前景到白色的连续 RGB 渐变，Alpha 合成保持独立。
- 未做 Blender 视口人工验收，未构建产物。
