## 1. 共用条带判定

- [x] 1.1 在 `tools/nodes/common/depth_surface.py` 实现四边面对边界边判定，在 Split Edges 后、启用分支 Switch 前接入单次 Face / All 删除；正式逻辑固定启用，无额外开关。
- [x] 1.2 验证孤立四边面、一面宽条带、普通边缘与角落、面角起点和绕序、三角面及其他多边面、非流形边；加入首次删除后出现新候选面的用例以确认仅执行一遍。

## 2. 两组接口与行为接入

- [x] 2.1 将 `O Image Depth Plane` 和 `O Image Depth Cutout` 的 Split Threshold 移到主输入区 Smooth 紧前，保留输入语义，整理局部节点布局。
- [x] 2.2 更新接口顺序、Options 归属、唯一输入、无实验开关及相关修改器赋值测试。
- [x] 2.3 更新原有面数保留断言，分别验证两组的四边面清理、三角面保留和 Split 为零时原行为；确认原梯度选择与比例映射不变。

## 3. 几何与集成验证

- [x] 3.1 验证剩余表面的 UV、用户属性、临时属性清理、空结果、正厚度闭合以及 Smooth 后区域独立；确认厚度与 Smooth 不改变基础删面集合。
- [x] 3.2 使用真实 Blender 求值验证两组节点；分别记录四边网格与当前 Cutout 三角网格的结果，不能用四边实验替代正式三角输入验收。
- [x] 3.3 运行 `uv run pytest` 全量测试并确认正常退出，检查最终差异和旧接口断言，保留其他已有修改；不构建文档或产物，并在交付说明中明确正式节点资产尚待另行重建发布。验证结果：940 passed、2 xfailed，退出码 0。
