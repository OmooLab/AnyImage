## 1. 基线与规范

- [x] 1.1 核对实施时工作区已有修改、七个几何组及嵌套引用，记录 Capture/Store 数量、各存值字段的域、阶段、实际消费者和必要性；运行现有节点测试，记录基线及已有失败。
- [x] 1.2 保存代表场景的几何、UV、材质索引与属性行为基线，覆盖深度切分、厚度、边界平滑、重叠表面、两种 Cutout 模式与 Panorama。
- [x] 1.3 更新 AGENTS.md、docs/internals/node-assets.md 及实际受影响的当前内部说明，统一禁止 Capture、字段直连、显式域转换、源几何采样与必要命名存储规则；核对当前规则一致。

## 2. 几何节点整理

- [x] 2.1 整理原边界与 Influence：Panorama 原边界使用闭合源面的常量语义，拓扑不变的字段直接求值，其余保护数据选择必要传递方式；验证原轮廓保护与平滑影响范围。
- [x] 2.2 整理平滑循环的 Boundary、SourceIndex、BoundaryIndex，明确稳定源几何与映射，移除 Capture；验证迭代前后映射、边界运动和拓扑行为。
- [x] 2.3 整理共用深度切分的 Camera、Cut，保持面采样、角域及点域转换与切分前后语义；验证非均匀深度、Split 开关和删面。
- [x] 2.4 整理厚度构建的源位置、顶点身份及法线，简化几何阶段或源采样，论证剩余必要命名数据；验证零厚度、封口、法线及独立重叠表面。
- [x] 2.5 整理 Depth Cutout 的中心、结构、圆化、厚度和膨胀字段，减少跨阶段存值；验证 Balloon/Uniform、厚度连续性、Edge Round、Front Inflation 和 Normal Smooth。
- [x] 2.6 整理材质继承的来源选择，移除 Source 捕获；验证 Image Plane、Panorama 的空输入、多材质槽、材质索引及生成几何保留。
- [x] 2.7 完成所有几何组和三个材质消费者的复查，保留 UVMap、o_normal_scale 等必要协议，逐项审查重复 Store；清理临时命名数据并验证用户属性保留。
- [x] 2.8 删除 capture_fields 及失去用途的辅助函数、导入和节点相关支持项；沿实际调用链确认旧实现引用消失，使用明确节点或 socket 引用接线。

## 3. 测试与可读性

- [x] 3.1 将 Capture 禁用检查加入构建定义及保存资产测试，递归遍历嵌套几何组并去重；更新 Store 名称规则以反映逐项批准的必要存储。
- [x] 3.2 替换 test_depth_relief_planes、test_boundary_smoothing 中强制 Capture 的断言，移除 test_depth_cutout 等测试辅助图中的 Capture 用法，保留有效行为覆盖与状态隔离。
- [x] 3.3 补齐本次受影响的域转换、边界固定映射、临时属性清理和用户属性保留测试；运行相关节点、材质及 Operator 测试并修复回归。
- [x] 3.4 逐组检查完整节点图的消费关系、跨区连线、交叉、重叠和接口显示，按需调整布局；验证排列前后实际连接与求值一致。

## 4. 资产与交付

- [x] 4.1 运行 uv run node-group build，更新 O_AnyImage.blend，完成保存后独立 Blender 加载与几何验证，确认全部几何组及嵌套组 Capture 为零。
- [x] 4.2 运行 uv run pytest 并确认正常退出；对比实施前行为基线，检查接口、几何、UV、材质及用户属性。
- [x] 4.3 检查最终差异、旧引用与当前规范一致性，交付逐组调整和保留 Store 的必要性清单、数量及验证结果；确认源码、规范、测试和资产同步，保留已有其他修改，不构建文档或打包扩展。
